git satus
git add xxx
git status
git commit -m "comments"
git log
git push -u origin master


git pull origin master
/**************************/
git branch -------------> show all branches 

git branch weida

git push origin weida

git checkout weida

modify

git add xxx

git commit -a -m "asdfasdfsa"

git checkout master

git merge weida

git branch -d weida
